import AppStyles from "../../config/AppStyles";

const TextInputsStyles = {
  textInput: {
    borderRadius: 0,
    borderWidth: 2,
    padding: AppStyles.sizes.paddingHalf,
    marginTop: AppStyles.sizes.marginDefault,
    marginBottom: AppStyles.sizes.marginDefault,
    fontFamily: AppStyles.fonts.secondaryFont,
    backgroundColor: AppStyles.colors.dexwinSmokeWhite
  }
};

export default TextInputsStyles;
